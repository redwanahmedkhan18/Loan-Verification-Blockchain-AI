import type { VerificationProvider } from "./types.js";
import type { Dispatcher } from "@nomicfoundation/hardhat-utils/request";
import type { VerificationProvidersConfig } from "hardhat/types/config";
export declare const BLOCKSCOUT_PROVIDER_NAME: keyof VerificationProvidersConfig;
export declare class Blockscout implements VerificationProvider {
    #private;
    name: string;
    url: string;
    apiUrl: string;
    constructor(blockscoutConfig: {
        name?: string;
        url: string;
        apiUrl: string;
        dispatcher?: Dispatcher;
    });
    getContractUrl(address: string): string;
    isVerified(address: string): Promise<boolean>;
    verify(contractAddress: string, sourceCode: string, contractName: string, compilerVersion: string, constructorArguments: string): Promise<string>;
    pollVerificationStatus(guid: string, contractAddress: string, contractName: string): Promise<{
        success: boolean;
        message: string;
    }>;
}
//# sourceMappingURL=blockscout.d.ts.map